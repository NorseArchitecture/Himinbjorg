<Project Sdk="Microsoft.NET.Sdk">
	<ItemGroup>
		<NorseDesignRef Include="Persistence.EntityFramework.Design">
			<Repo>Urdarbrunnr</Repo>
		</NorseDesignRef>
		<!-- Needed directly, not just transitively via the ProjectReference below: it marks its own
		     NorseDesignRef to this package PrivateAssets="all" (correctly, so runtime consumers of
		     the Migrations package don't inherit design-time tooling), which also blocks it from
		     flowing here. NorseIdentityMigrationContributor derives from a Design-chassis base type
		     living in the same assembly this test references directly. -->
		<ProjectReference Include="../../src/Identity.Migrations/Identity.Migrations.csproj" />
	</ItemGroup>
</Project>
