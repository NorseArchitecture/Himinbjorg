# Norse.Identity

ASP.NET Core Identity v3 entity types, `NorseIdentityDbContext` (Identity + OpenIddict), `NorseUserStore` with projection overrides, and a DI extension. Runtime library — referenced by `Norse.Auth.Server`; never by migration tooling.

Part of the [Norse Architecture](https://github.com/NorseArchitecture) platform.
