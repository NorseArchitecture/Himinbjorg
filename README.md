# Himinbjorg
Himinbjörg — Heimdall's hall at the head of Bifrost. The backend-only identity store of the Norse Architecture: EF Core model, conventions, and migrations for ASP.NET Identity and OpenIddict (Norse.Identity.*). Entities stay server-side and never cross into WASM or MAUI.
